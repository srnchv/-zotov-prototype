# Цифровой архив Центра «Зотов» — прототип. Хэндофф

Снимок состояния проекта для продолжения работы (в Cowork / Claude Code / другой сессии).
Обновлено: 2026-07-22.

---

## 1. Что это

UX клик-прототип **публичной части** digital-архива Центра «Зотов» (для дизайн-студии Charmer).
Демонстрирует логику из ТЗ «**Проектное решение. Digital архив**».

- Продукт в бою: **связная база знаний** (не сайт-каталог) на **WordPress**, хостинг REG.RU, до ~1000 ТБ.
- Главный принцип ТЗ: объект создаётся **один раз** → автоматически выводится во всех релевантных
  разделах через **двусторонние связи многие-ко-многим**. Редактор ничего не дублирует руками.
- Прототип — статический SPA (без сборки), показывает эту логику интерактивно.

## 2. Где что лежит

| Что | Где |
|---|---|
| Код прототипа | `zotov-proto/` — `index.html` + `style.css` + `app.js` (SPA, hash-роутинг) |
| Запуск локально | `python3 -m http.server 5050 --directory zotov-proto` → http://localhost:5050 (или preview-конфиг `zotov-proto` в `.claude/launch.json`) |
| GitHub | репозиторий `srnchv/-zotov-prototype`, ветка `main` |
| Live-сайт | https://srnchv.github.io/-zotov-prototype/ (GitHub Pages, пересборка ~1 мин после push) |
| Публикация | токен (fine-grained, Contents R/W) в macOS Keychain → `git push origin main` работает молча. Нет gh/brew/ssh — только https+токен |
| ТЗ | «Проектное решение. Digital архив» (полный текст присылался в чат). Разделы: 1 общая логика, 2–14 сущности, 15 связи, 16 сценарии, 17 справочники, **18 поиск**, **19 публичные разделы**, **20 личный кабинет**, 21 техтребования |
| Брендбук | `~/Downloads/Zotov Brandbook 2026.pdf` (31 стр., визуальный; читать через python `fitz`/PyMuPDF) |
| Figma-вайрфреймы | файл Presentations `61NzpKqxaiJd188Ux1AGQn`, страница «МТС» (КП 21 слайд, прототипы публички, схемы, кабинеты ролей) |

## 3. Архитектура `app.js`

- **Данные:** массив `RAW` (сущности + поле `links`), при init связи делаются двусторонними → индекс `DB`.
  Наполнение: материалы `m1–m12`, личности `p1–p4` (в т.ч. Дзига Вертов), места `pl1–pl3` (Москва),
  события `e1–e2`, темы `t1–t2`, проекты `pr1–pr2` (выставка «Киноглаз»), организации `o1–o2`,
  коллекции `c1`/`c2` (Коллекция 1/2), источники `s1–s2`, теги, медиа. У материалов есть
  `mtype`/`subtype`/`date`/`access`/`media[]`/`a11y[]`/`lang`.
- **Роутер `render()`:** hash формата `#/path?query`. Разделы: `home`, `archive` (поиск/выдача),
  `chrono`, `map`, `cat/<type>` (каталоги), `library`, `e/<id>` (карточка сущности),
  `cabinet/<tab>` (ЛК), `request` (модалка заявки). Скролл вверх только при смене раздела.
- **Поисковый движок:** `parseFilt/buildHash` (URL ↔ фильтры), `runSearch/passes/sortItems`.
  Состояние фильтров — множества в `FILT` (тип, подтип, десятилетие, доступ, медиа, доступность,
  язык + 9 связей-сущностей). Всё состояние в URL → воспроизводимая ссылка.
- **Карточки сущностей:** `relatedSections(o)` (связи: материалы/личности/места/события/проекты/
  коллекции — карточками; темы/орг/источники/теги — чипами) + `similarBlock(o)` (похожие материалы
  по общим связям). Без дублирования.
- **ЛК:** `cabinet(tab)` + `cab*`-рендереры. Живое через localStorage:
  `zotov_savedmat` (сохранённые материалы), `zotov_hist` (история), `zotov_coll` (подборки),
  `zotov_saved` (сохранённые поиски).

## 4. Что реализовано (всё проверено в браузере)

1. **Главная v2** — блочная структура по Figma-вайрфрейму: hero → поиск + чипы популярных тем →
   Хронограф (мини-шкала + 2 события) → Личности (портреты) → Карта+Коллекции (асимметрия, у каждой
   колонки свой заголовок с кнопкой) → Темы (большой+малый материал) → Проекты/Выставки.
2. **Поиск (ТЗ разд. 18):** 2 входа → 1 выдача («Архив / поиск»). Простой + расширенный,
   многомерная фильтрация чипами с множественным выбором, поиск по связям («Вертов» → связанные
   материалы), группировка результатов по типам, сортировка, URL-состояние, «Скопировать ссылку»,
   сохранение поиска в ЛК, экспорт CSV.
3. **Разделы-каталоги** + компактный поиск внутри раздела (`sectionSearch`).
   **Библиотека** (`#/library`) = материалы типов «Печатная продукция»/«Текст» (по ТЗ).
4. **ЛК — 8 вкладок** (Профиль, Сохранённые материалы, Сохранённые поиски, Личные подборки,
   Заявки на доступ, История просмотров, Уведомления, Настройки).
5. **Навигация** — единый пункт «Архив / поиск» (убран дубль «Поиск ⌕»).

## 5. Ключевые решения

- **Визуал:** нейтральный серый вайрфрейм (шрифт Inter). Бренд Зотова (красный `#F43C3A`,
  дисплейный шрифт) **пробовали и откатили** — фирменные шрифты Зотова НЕ внедряем без явной просьбы.
- **GitHub:** публикация через https + Personal Access Token (в Keychain), push в `main` → Pages.
- **Упрощения прототипа:** уровни доступа 3 значения (в ТЗ 9); подтип — фильтр подстрокой;
  экспорт CSV (в ТЗ основной XLSX); данные демонстрационные.

## 6. Открытые задачи

- Поиск: точная дата/диапазон/«около» (сейчас живут только десятилетия), автоподсказки,
  права доступа в выдаче, поиск внутри разделов на страницах сущностей.
- Уровни доступа и подтипы — до полного справочника ТЗ.
- Экспорт XLSX (сейчас CSV).
- **Админ-контур** (карточка-редактор сущности, очередь модерации, справочники, журнал изменений) —
  есть только в Figma-схемах, в HTML-прототипе нет.
- Мобильная адаптация.
- Синхронизация Figma-вайрфрейма с текущим HTML (разошлись: HTML новее — поиск, ЛК, главная v2).

## 7. Как продолжить

```bash
cd "zotov-proto"
python3 -m http.server 5050 --directory .     # или preview-конфиг zotov-proto
# правки в app.js / style.css
git add -A && git commit -m "…" && git push origin main   # публикует на Pages
```

История коммитов (main) отражает эволюцию: единая модель поиска → живые фильтры → поиск по ТЗ →
вкладки ЛК → разделы/карточки без дублей → главная v2 → блок Карта·Коллекции → навигация.
